[project]
name = "clawlink-visual-gui"
version = "1.0.0"
description = "Commercial-grade Web GUI for ClawLink agent management"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
